import { Body, Controller, Param, Post } from '@nestjs/common';import { PaymentsService } from './payments.service';@Controller('payments')export class PaymentsController{constructor(private svc:PaymentsService){}@Post()create(@Body()b){return this.svc.createPayment(b);}@Post(':provider/:paymentId/capture')capture(@Param('provider')p,@Param('paymentId')id,@Body('amount')amount){return this.svc.capture(p,id,amount);} }