import { Injectable } from '@nestjs/common';import { RazorpayProvider } from './razorpay/razorpay.provider';import { CashfreeProvider } from './cashfree/cashfree.provider';import { PayuProvider } from './payu/payu.provider';@Injectable()export class ProviderFactory{constructor(private rzp:RazorpayProvider,private cf:CashfreeProvider,private payu:PayuProvider){}get(name:string){switch(name.toLowerCase()){case 'razorpay':return this.rzp;case 'cashfree':return this.cf;case 'payu':return this.payu;default:throw new Error(`Unknown provider: ${name}`);}}}