import { PrismaClient } from '@prisma/client';const prisma=new PrismaClient();async function main(){console.log('Reconciling...');const rows=await prisma.transaction.findMany({ where:{ status:{ in:['created','pending'] } }, take:100 });for(const t of rows){console.log('Would reconcile', t.id, t.providerTxId);}await prisma.$disconnect();}if(require.main===module){main().catch(e=>{console.error(e);process.exit(1);});}