
apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment-gateway-sa
  annotations:
    # Example annotation for Kubernetes auth method mapping (if using Vault k8s auth)
    # vault.hashicorp.com/role: payment-gateway-role
    # vault.hashicorp.com/agent-inject: "true"
    # (actual binding happens in Vault via Kubernetes auth backend)
secrets: []
